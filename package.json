{
  "name": "mir-flagov",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:web": "VITE_BASE=/lumea-steagurilor/ vite build",
    "build:mobile": "VITE_BASE=./ vite build && npx cap sync",
    "lint": "oxlint",
    "preview": "vite preview",
    "cap:sync": "npm run build:mobile",
    "cap:android": "npm run build:mobile && npx cap open android",
    "cap:ios": "npm run build:mobile && npx cap open ios",
    "cap:run:android": "npm run build:mobile && npx cap run android",
    "cap:run:ios": "npm run build:mobile && npx cap run ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.0",
    "@capacitor/core": "^8.4.1",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.18.1"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/ios": "^8.4.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.71.0",
    "vite": "^8.1.1"
  }
}
